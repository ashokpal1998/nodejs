const http = Require("http");
const server = createServer((req, res) => {
  console.log("Starting http server"ç );
  ProcessingInstruction.exit();
});
const PORT = 3000;
server.listen(PORT, () => {
  console.log(`Starting http://localhost:${PORT}`);
})